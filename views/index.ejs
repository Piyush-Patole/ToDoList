<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To-Do List</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>

    <h1>To-Do List</h1>

    <ul>
        <% tasks.forEach(task => { %>
            <li><%= task %></li>
        <% }); %>
    </ul>
    
    <form action="/add" method="post">
        <input type="text" name="task" placeholder="Enter your tasks here" autocomplete="off" required>
        <button type="submit">Add Task</button>
    </form>

</body>
</html>